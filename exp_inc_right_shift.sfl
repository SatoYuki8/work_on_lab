circuit exp_inc_right_shift{
    input exp<5>;
    input frac<13>;
    output inc_exp<5>;
    output shifted_frac<13>;
    instrin do;

    sel_v tmp<14>;

    instruct do par{
	inc_exp = exp + 0b00001;
	tmp = (0b1 || frac) >> 0b1;
	shifted_frac = tmp<12:0>;
    }
}
